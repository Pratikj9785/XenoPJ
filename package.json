{
  "name": "xeno",
  "version": "1.0.0",
  "description": "Xeno Analytics Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
